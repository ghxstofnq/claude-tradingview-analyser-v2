# Oracle packet — 2026-06-24 NY-AM

## Status

Status: **APPROVED MNQ B INVERSION LONG — VALID FIRST TRADE, STOPPED; SECOND-ENTRY POLICY BLOCKER DOCUMENTED**.

User approval on 2026-07-02:

- The first MNQ B Inversion long is valid and should be approved as a losing trade.
- The important lesson is not to rewrite the first trade as a win; it is to document why the bot did not take the later second long that probably would have won.

## Source coverage

Fresh re-recorded tapes exist for both instruments and are warning-clean:

| Instrument | Fresh tape | HTF coverage | Warnings |
|---|---|---|---:|
| MNQ | `tests/tapes/fresh-oracle/2026-06-24-mnq-ny-am-replay.tape.json` | Daily/H4/H1/M15/M5 present | 0 |
| MES | `tests/tapes/fresh-oracle/2026-06-24-mes-ny-am-replay.tape.json` | Daily/H4/H1/M15/M5 present | 0 |

The old committed tape `tests/tapes/2026-06-24-ny-am.tape.json` remains `verified:false`: it is a stale/incomplete legacy window with missing HTF digest. Its expected row has been updated to point at the approved current packet, but the scored outcome comes from the local fresh tape.

## Approved packet

| Field | Value |
|---|---|
| Instrument | MNQ |
| Model | Inversion long |
| Grade | B |
| Event time | `2026-06-24T09:51:00-04:00` |
| Entry | `29722.25` |
| Structural stop anchor | `29564` |
| Executable stop | `29563.5` |
| Stop buffer | 2 ticks beyond anchor |
| TP1 | `29843.5` |
| TP2 | `29874` |
| Outcome | `stop_hit` |

## Outcome path

| Event | NY time | Evidence |
|---|---|---|
| Entry / first `packet_ready` | `09:51 EDT` | `Inversion long B`, entry `29722.25` |
| Stop hit | `10:20 EDT` | low through executable stop `29563.5` |
| Later TP1 area | `10:46 EDT` | high `29849.75`, through TP1 `29843.5` |
| Later TP2 area | `10:48 EDT` | high `29877.75`, through TP2 `29874` |

Correct scoring: **loss / -1R** for the first trade.

## Pair context

| Signal | Result |
|---|---|
| Displacement leader | `null` / inconclusive (`0.86` vs `0.88`, margin `0.02 < 0.10`) |
| SMT | divergence true, leader `MNQ1!`, bias_dir `short` |
| MES counterpart | context none / no setup; brief reason `no_bias`, pillar2 `marginal` |

This is an MNQ-specific approval, not a pair-leader rule.

## Why no second trade?

Trace answer: **the later entries were blocked by the one-primary-packet session latch**.

Production trace of the fresh MNQ tape:

```text
actual bestPacket count: 1
packet_ready events: 1
confirmed events: 16
session_primary_already_taken kills: 14
```

The first packet:

```text
09:51 EDT packet_ready Inversion long B
entry 29722.25 / executable stop 29563.5 / TP1 29843.5
```

Later confirmed-only candidates included:

```text
10:32 EDT Inversion long — killed: session_primary_already_taken
10:47 EDT Inversion long — killed: session_primary_already_taken
10:48 EDT Inversion long — killed: session_primary_already_taken
```

The 10:32 long is the important review note: it came after the 10:20 stop and before the 10:46/10:48 TP run. User notes that this second entry would probably have been valid and won. Current engine policy does not permit it because once a walker reaches `packet_ready`, later confirmed walkers in the same NY-AM session are killed as continuations of the already-selected primary trade.

## Resolution

- Approve the **first** MNQ B Inversion long as a valid losing oracle trade.
- Preserve the likely winning second long as **implementation review evidence**, not as an executable oracle row yet.
- Future work, if desired: design and test a controlled `retry_after_true_execution_stop` rule. It must be implemented deliberately; do not force-green the oracle by pretending the current bot took that second trade.
