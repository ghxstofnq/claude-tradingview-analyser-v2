# Oracle packet — 2026-06-24 NY-AM

## Status

Recommendation: **REVIEW — APPROVE CURRENT PRODUCTION-FOLD PACKET OR EXTEND TAPE BEFORE SCORING**.

This is the only tracked recent non-Batch-A MNQ day tape currently present in the workspace. It is replayable, but still `verified:false` because its stored expectation is stale relative to the current deterministic fold and the tape window ends before the current packet resolves to TP/stop.

Do **not** flip this tape to `verified:true` or include it in pair-leader R scoring until the user approves the packet and the outcome window is either extended or explicitly accepted as unresolved/packet-only.

## Evidence sources

- `tests/tapes/2026-06-24-ny-am.tape.json`
- Current production fold via `foldTape(... buildDeterministicPacketTruthFromInputs ...)` on 2026-06-30
- `docs/strategy/lanto-oracle.md` has **no canonical 2026-06-24 row** yet

## Current tape state

| Source | State |
|---|---|
| Tracked MNQ tape | `verified:false`, 28 bars, 13:45–14:12 UTC |
| Stored expectation | `Inversion long`, entry `29683.25`, stop `29604`, TP1 `29843.5`, first packet `2026-06-24T13:46:00.000Z`, no grade |
| Current production fold | `Inversion long B`, entry `29722.25`, stop `29564`, TP1 `30046.75`, first packet `2026-06-24T13:51:00.000Z`, one distinct opportunity |
| Outcome inside tape window | unresolved: post-entry max high `29790`, min low `29626`; neither TP1 `30046.75` nor stop `29564` is touched before tape end |

## Candidate packet proposed for approval

From the current deterministic fold:

| Field | Value |
|---|---|
| Instrument | MNQ |
| Outcome | trade packet surfaced; outcome unresolved in current tape window |
| Model | Inversion long |
| Grade | B |
| Entry | `29722.25` |
| Stop | `29564` |
| TP1 | `30046.75` |
| First packet timestamp | `2026-06-24T13:51:00.000Z` |
| Distinct opportunities | 1 (`Inversion:long`) |
| Why B | Current chain emits B; no approved canonical row supports A+ elevation |

## Discrepancy

The stored unverified expectation appears to be an older first-packet prefill (`29683.25 / 29604 / 29843.5`) that the current production chain no longer emits. The current chain emits only one distinct opportunity, but at the later/higher packet above.

## Approval question

Approve replacing the stale 06-24 stored expectation with the current production-fold packet above?

Recommended approval shape:

1. Approve the packet fields as **candidate oracle packet**.
2. Keep `verified:false` until the tape is extended far enough to score outcome, or explicitly mark this as packet-only evidence.
3. Do not use 06-24 in pair-leader R totals until outcome is resolved and, if pair scoring is needed, an MES counterpart exists.
